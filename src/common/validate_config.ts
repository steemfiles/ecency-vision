// This module is soley for validating that the configurations are correct.
//import {TEST_NET, APP_NAME, APP_NAME_LATIN, APP_NAME_UPPERCASE, APP_ICON, APP_URL, APP_DOMAIN, APP_ADMIN_HIVE, APP_ADMIN_EMAIL} from "../client_config";
//import {APP_MAX_TAG, SCOT_TAG, TAG_LIST, TOKEN_IMAGE, LIQUID_TOKEN, LIQUID_TOKEN_UPPERCASE} from "../client_config";
